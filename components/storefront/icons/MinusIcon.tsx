'use client';

import type { IconBaseProps } from 'react-icons';
import { FiMinus } from 'react-icons/fi';

const MinusIcon = (props: IconBaseProps) => <FiMinus {...props} />;

export default MinusIcon;
